# Kubeconfig gerado pelo kind (contem credenciais do cluster)
garageos-config

# Terraform
# Obs: .terraform.lock.hcl NAO e ignorado de proposito - deve ser versionado
# (trava as versoes dos providers para todos os ambientes e para o CI/CD).
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
override.tf
override.tf.json
